services:
  postgres:
    image: postgres:16-alpine
    container_name: club_db
    environment:
      POSTGRES_DB: clubdb
      POSTGRES_USER: clubuser
      POSTGRES_PASSWORD: clubpass123
    ports:
      - "5432:5432"
    volumes:
      - club_data:/var/lib/postgresql/data

volumes:
  club_data: